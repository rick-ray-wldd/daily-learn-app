# Echo — 環境變數（全部選填）
# 複製成 .env 後填值；改完記得重啟 dev server（npx expo start -c）。
# EXPO_PUBLIC_ 前綴是 Expo 的規定：只有這個前綴的變數會被打包進 client bundle。
#
# ✅ ADR-0008 已完成：Whisper / Claude 的 key 已經移到 Supabase Edge Functions，
#    client 端不再帶任何 provider key。這個檔案裡只剩下「本來就該公開」的值。

# --- Supabase（選填）：不設也能跑（本地模式，資料只存手機） ---
# Supabase 專案 URL，例如 https://abcdefghijkl.supabase.co
EXPO_PUBLIC_SUPABASE_URL=

# Supabase anon (public) key — 設計上就是可公開的 client key，不是 service_role。
# 它進 bundle 沒關係：真正的保護是 RLS（migration 002：auth.uid() = user_id）
# 加上 Edge Function 會拒絕「只有 anon key、沒有使用者」的呼叫。
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# =============================================================================
# 以下是 **server 端** secrets，不放這裡、也不放 .env。
# 用 Supabase CLI 設定（或 Dashboard → Edge Functions → Secrets）：
#
#   supabase secrets set OPENAI_API_KEY=sk-...        # Whisper 逐字稿
#   supabase secrets set ANTHROPIC_API_KEY=sk-ant-... # Claude 難點診斷
#   supabase secrets set DIAGNOSE_MODEL=claude-haiku-4-5   # 選填，這是預設值
#
# SUPABASE_URL / SUPABASE_ANON_KEY / SUPABASE_SERVICE_ROLE_KEY 由平台自動注入，
# 不需要也不應該手動設定。
# =============================================================================
